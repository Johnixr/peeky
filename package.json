{
  "name": "peeky",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Peeky — a transparent floating macOS desktop AI companion built with Tauri 2.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vite build",
    "ci:local": "pnpm run typecheck && cd src-tauri && cargo test --locked && cd .. && pnpm run ci:ocr-runtime",
    "ci:ocr-runtime": "node scripts/check-ocr-runtime.mjs",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-global-shortcut": "^2.0.0",
    "@tauri-apps/plugin-opener": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
